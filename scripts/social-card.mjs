import { mkdir, writeFile } from "node:fs/promises";\nimport sharp from "sharp";

const esc=v=>String(v??"").replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
const wrap=(text,max=58)=>{const words=String(text||"").split(/\s+/),lines=[];let line="";for(const word of words){if((line+" "+word).trim().length>max&&line){lines.push(line);line=word}else line=(line+" "+word).trim()}if(line)lines.push(line);return lines.slice(0,4)};
export const cardPath=(date,storyId,format="landscape")=>"assets/social-cards/"+date+"/"+storyId.replace(/[^a-z0-9-]+/gi,"-").slice(0,80)+"-"+format;
export const createCard=async(post,date,format="landscape")=>{const portrait=format==="instagram",w=portrait?1080:1200,h=portrait?1350:675,lines=wrap(post.headline,portrait?30:58),source=post.source_name||"News",partner=post.detected_partner?post.detected_partner.toUpperCase():"AI NEWS",base=cardPath(date,post.headline,format),svgPath=base+".svg",pngPath=base+".png",headline=lines.map((x,i)=>"<tspan x=\"80\" dy=\""+(i?62:0)+"\">"+esc(x)+"</tspan>").join(""),svg=`<svg xmlns="http://www.w3.org/2000/svg" width="${w}" height="${h}" viewBox="0 0 ${w} ${h}"><defs><linearGradient id="g" x1="0" y1="0" x2="1" y2="1"><stop stop-color="#111827"/><stop offset="1" stop-color="#2563eb"/></linearGradient></defs><rect width="${w}" height="${h}" fill="url(#g)"/><circle cx="${w-140}" cy="110" r="240" fill="#38bdf8" opacity=".16"/><circle cx="${w-80}" cy="${h-30}" r="190" fill="#a78bfa" opacity=".12"/><text x="80" y="78" fill="#93c5fd" font-family="Arial,sans-serif" font-size="25" font-weight="700" letter-spacing="4">MAHGPT  /  AI NEWS</text><text x="80" y="${portrait?280:245}" fill="white" font-family="Arial,sans-serif" font-size="${portrait?48:45}" font-weight="700">${headline}</text><rect x="80" y="${portrait?570:470}" width="220" height="42" rx="21" fill="#38bdf8"/><text x="190" y="${portrait?598:498}" text-anchor="middle" fill="#0f172a" font-family="Arial,sans-serif" font-size="20" font-weight="700">${esc(partner)}</text><text x="80" y="${h-78}" fill="#dbeafe" font-family="Arial,sans-serif" font-size="24">Source: ${esc(source)}  •  MahGPT</text></svg>`;await mkdir(svgPath.slice(0,svgPath.lastIndexOf("/")),{recursive:true});await writeFile(svgPath,svg);await sharp(Buffer.from(svg)).png().toFile(pngPath);return pngPath};
