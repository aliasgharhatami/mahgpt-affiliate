// Zero-cost provider contract. Default mode is static and never calls a network service.\nexport function translateNewsItem(item, locale, options = {}) {\n  if ((options.mode || "static") !== "static") throw new Error("External translation providers are disabled by default.");\n  return null;\n}\nexport const translationProvider = { mode: "static", externalEnabled: false, requiresSecrets: false };\n