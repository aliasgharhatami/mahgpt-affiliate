name: Test MahGPT Telegram publisher
on:
  workflow_dispatch:
    inputs:
      source:
        description: Optional exact source URL from assets/news-feed.json; leave blank to test the first current item
        required: false
        type: string
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Send exactly one controlled test post
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHANNEL: "@mahgptplus"
          TEST_SOURCE: ${{ inputs.source }}
        run: node scripts/social-publisher.mjs test
